export default function productId({params}){
    return(

        <>
        <h1>PRODUCT IDS{params.productId}</h1>
        </>
    )
}

// next js react for the folder name of square brace in dynamic segment .  params id containe the parameter as
//key value pairs in key of product id route have single parameter that is product id 

//first created a folder in square [] and id with in the [].
//then created page.js file inside the folder this allow map any route by url /poducts/id